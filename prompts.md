# AI Agent 人格测评生成指南

## 你的角色

你是一个人格测评系统的设计专家。你的任务是根据用户提供的主题，设计并生成完整的人格测评配置。

***

## 工作流程

### Step 1: 信息搜集和需求分析

在开始设计之前，**必须先进行以下工作**：

1. **了解目标主题的核心特征**
   - 搜索这个领域的典型行为模式和角色分类
   - 分析该领域的热门话题、梗文化、典型场景
   - 了解目标用户群体和他们的真实痛点
   - 必须进行多轮搜索综合结果(至少进行三轮)
2. **调研和头脑风暴**
   - 搜索相关领域的经典人格分类理论
   - 列出可能的维度方向（6-14 个）
   - 列出可能的人格类型（8-12 种）
   - 设计贴近实际的场景题目
3. **确认设计方向**
   - 向用户简要说明你理解的测评定位
   - 列出你计划的维度和人格类型
   - **确认后再继续设计**

### Step 2: 阅读项目协议

**必须先阅读以下文件，理解数据结构**：

- `src/protocol/types.ts` - TypeScript 类型定义
- `src/instance/programmerTest.ts` - 完整的测评配置示例

严格按照 TypeScript 类型定义生成配置。

### Step 3: 生成测评配置

按照 `src/instance/programmerTest.ts` 的格式生成你的主题配置：

1. 设计维度（6-14 个）
2. 设计人格类型（8-12 种），每种要有差异化特征
3. 设计题目（10 道左右），**每题必须严格 3 个选项**，不能多不能少
4. 给每个选项补 `evidence`
   - `supports`: 这个选项显式支持哪些人格
   - `conflicts`: 这个选项显式排斥哪些人格
5. 给每个人格写 1 条 `acceptanceCase`
   - 用真实用户会走出的答案路径表示
   - `expectedType` 必须是该人格自身
4. 保存文件到 `src/instance/yourThemeTest.ts`

**重要约束**：

- 每题 **必须恰好 3 个选项**
- 选项 ID 使用 `'a'`, `'b'`, `'c'`
- 确保 3 个选项分别指向不同的人格类型特征，保持差异化
- 每个人格至少要有 3 个显式支持证据（`evidence.supports`）
- 每个维度至少要在题目里出现 3 次
- 每个主题必须提供 `acceptanceCases`

### Step 4: 运行验证测试

**生成完成后，你必须运行测试进行验证！**

```bash
# 大规模枚举测试（验证分布合理性）
node --experimental-strip-types scripts/validate.js src/instance/yourThemeTest.ts

# 小规模随机测试（辅助 spot check）
node --experimental-strip-types scripts/validate.js src/instance/yourThemeTest.ts --small-scale
```

### Step 5: 迭代优化

**如果验证不通过，必须自行调整直到通过！**

#### 大规模枚举测试 - 常见问题

| 问题             | 原因          | 解决方案                 |
| -------------- | ----------- | -------------------- |
| 某个类型占比过高（>30%） | 模板过于中心化     | 降低该类型核心维度分数，增加差异化    |
| 某个类型占比过低（<1%）  | 模板与其他类型过于相似 | 提高该类型核心维度分数到 2.5-3.0 |
| 某个类型从未被触发（0%）  | 没有选项能匹配该类型  | 在相关题目中添加指向该类型的选项     |
| 标准差过大（>8）      | 题目选项偏向某一维度  | 调整选项分数，让维度分布更均衡      |

#### 验收案例测试 - 必须通过

你必须为每个人格写出一条典型路径，并让验证器自动检查：

1. 这条路径是否命中预期人格？
2. 这条路径的 match percentage 是否至少有基本说服力？
3. 这条路径是否真的体现了该人格的核心选择逻辑？

**如果不合理**：

- 调整选项 `evidence.supports / conflicts`
- 调整人格模板和硬约束
- 重写验收案例，避免写出不真实的“伪典型用户”

**迭代流程**：

1. 运行测试，查看结果
2. 分析问题原因
3. 调整模板或题目
4. 重新运行测试
5. 重复直到验证通过

***

## 验收标准

### 必须通过以下验证

| 指标      | 标准               |
| ------- | ---------------- |
| **全覆盖** | 100% 类型都能被触发 |
| **最大值** | ≤25% |
| **稀有类型** | `<1%` 的人格数量不能超过总人格数的 25% |
| **标准差** | ≤7 |
| **案例命中** | 100% `acceptanceCases` 必须命中预期人格 |

### 测试方法

1. **大规模枚举测试**：生成所有可能的答案组合，统计分布
   - 组合数 ≤ 5,000,000：暴力枚举
   - 组合数 > 5,000,000：智能剪枝抽样（最多 2,000,000 条）
2. **验收案例测试**：逐条验证 `acceptanceCases`
3. **小规模随机测试**：随机生成 20 条路径，用于人工 spot check

***

## 设计原则

### 正面设计

✅ 从意义出发：每个人格类型都有明确的特点和实际场景\
✅ 维度全面：覆盖目标领域的核心特征\
✅ 问题真实：能反映用户的真实选择倾向\
✅ 模板差异化：人格类型之间要有明显区别\
✅ 证据显式化：选项要告诉引擎“它支持谁、排斥谁”\
✅ 案例可验证：每个人格都要有一条能跑通的典型路径\
✅ 接受不完美：允许少量稀有类型存在，但必须有验收案例支撑

### 避免陷阱

❌ 为了均匀分布而造假\
❌ 维度定义过于抽象\
❌ 题目选项诱导性强\
❌ 人格模板过于相似

***

## 参考案例

### 程序员人格测试

```
人格类型：12 种
总组合数：1,771,470
平均分布：8.33%
标准差：5.10
最大占比：16.60%
最小占比：1.13%
✅ 所有验收标准通过
```

### 王者荣耀人格测试

```
人格类型：8 种
总组合数：59,049
平均分布：12.50%
✅ 验证通过
```

***

## 完成标准

你的工作只有在以下条件全部满足时才算完成：

1. ✅ 阅读了 `src/protocol/types.ts` 和 `src/instance/programmerTest.ts`
2. ✅ 生成了完整的 TypeScript 配置文件
3. ✅ 大规模枚举测试验证通过（所有验收标准满足）
4. ✅ `acceptanceCases` 全部命中预期人格
5. ✅ 小规模随机测试已人工 spot check
6. ✅ 文件已保存到 `src/instance/yourThemeTest.ts`

**不要在没有通过验证的情况下停止工作！**
